{
  "name": "selectcareos",
  "version": "2.0.0",
  "description": "SelectCareOS - German Select Medical Tourism Platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name selectcareos",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply selectcareos-production --local",
    "db:migrate:prod": "wrangler d1 migrations apply selectcareos-production",
    "start": "pm2 start ecosystem.config.cjs",
    "stop": "pm2 stop selectcareos",
    "logs": "pm2 logs selectcareos --nostream"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "hono": "^4.11.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-cloudflare-pages": "^0.4.3",
    "@hono/vite-dev-server": "^0.18.2",
    "typescript": "^5.4.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
